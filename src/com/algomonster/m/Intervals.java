package com.algomonster.m;

/**
 * For formula #1, If we want to go one-step further for,
 * we can reduce it by DeMorgan's Law (Not (A or B) == Not A and Not B),
 * this is equivalent to Not (x2 < y1) and Not (y2 < x1) which is equivalent to x2 >= y1 and y2 >= x1
 */
public class Intervals {

}
